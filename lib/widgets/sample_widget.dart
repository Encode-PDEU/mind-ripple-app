import 'package:flutter/cupertino.dart';

/// [SampleWidget] is a widget that displays a sample text.
/// This widget is used in the [LoginScreen].
/// Explain the purpose of this widget.
/// Mention dependencies if any.
/// Mention the usage of this widget.

class SampleWidget extends StatelessWidget {
  const SampleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Sample Widget');
  }
}
